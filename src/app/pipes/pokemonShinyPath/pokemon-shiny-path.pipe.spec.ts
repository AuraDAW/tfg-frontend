import { PokemonShinyPathPipe } from './pokemon-shiny-path.pipe';

describe('PokemonShinyPathPipe', () => {
  it('create an instance', () => {
    const pipe = new PokemonShinyPathPipe();
    expect(pipe).toBeTruthy();
  });
});
